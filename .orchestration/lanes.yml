# .orchestration/lanes.yml — indexa
# Rust workspace. verify matches ci.yml's actual steps exactly.
verify: "cargo fmt --all -- --check && cargo clippy --all-targets --all-features --locked && cargo test --all --locked"

hot_files:
  # Pure-addition edits by 2+ lanes — expect a trivial rebase, not a logic conflict.
  - crates/mcp/src/lib.rs                   # new-tool lane(s) register in tool_router()
  - crates/web/src/lib.rs                   # UI_JS/UI_CSS concat! lists — new fragments only
  - AGENTS.md                               # tool-count token; owned by whichever lane changes the MCP tool count
  - README.md                               # tool-count token; owned likewise
  - USAGE.md                                # tool-count token; owned likewise
  - docs/how-to/live-retrieval-over-mcp.md  # tool-count mention; owned likewise
  - CHANGELOG.md                            # every lane appends its own [Unreleased] bullet, never edits another's

counters:
  mcp_tool_count: 53
  ui_js_fragment_count: 32
  ui_css_fragment_count: 23

# 2026-08-30 six-lane round: retrieval-batcher-wiring, mcp-ledger-and-drift-tools,
# graph-layer-clustering-ui, pack-inclusion-modes-and-dryrun,
# docs-staleness-and-redundancy-audit, invariant-audit-and-small-fixes — all merged,
# v0.78.0 tagged. Their territory is released; superseded by the round below.
singleton_paths:
  # 2026-08-31 round-2 (four lanes) — exclusive per-lane territory.
  - crates/parsers/src/agent_sessions.rs    # agent-session-transcript-parser (new file)
  - docs/how-to/index-agent-session-history.md  # agent-session-transcript-parser (new file)
  - crates/query/src/eval.rs                # eval-answer-quality-judge
  - apps/indexa/src/commands/eval.rs        # eval-answer-quality-judge
  - docs/how-to/evaluate-retrieval.md       # eval-answer-quality-judge
  - apps/indexa/src/commands/plugin.rs      # plugin-directory (new file)
  - docs/plugins.toml                       # plugin-directory (new file, path may vary — see PR)
  - crates/llm/src/openai_compat.rs         # misc-provider-and-audit-followups (Cerebras wiring, read-mostly)
  - crates/parsers/src/compressed.rs        # misc-provider-and-audit-followups (zstd multi-frame fix)
  - apps/indexa/src/commands/update.rs      # misc-provider-and-audit-followups (non-interactive --yes gate)
  - docs/config.md                          # misc-provider-and-audit-followups (Cerebras provider row)

# release.yml triggers on version tags (v*.*.*), not on merge to main — confirmed by
# reading the trigger. No merge-triggered deploy exists for this repo.
deploy_on_merge: []

setup:
  run: null
  copy_env: []
  ports: {}

required_checks: [ci]
merge: squash
force_push: blocked
serialized_ci: []
